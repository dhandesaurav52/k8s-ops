Thank you for installing SkyOps Agent!

Release Name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

1. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. View live agent telemetry logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -f

3. SkyOps Server Endpoint:
   {{ .Values.server.url }}

4. To manage or upgrade this release:
   helm upgrade {{ .Release.Name }} ./deploy/chart --namespace {{ .Release.Namespace }} --set server.url="{{ .Values.server.url }}"
