SkyOps Agent has been successfully installed into your Kubernetes cluster!

Cluster Name: {{ .Values.cluster.name }}
SkyOps Server URL: {{ .Values.server.url }}

To check the status of your SkyOps Agent deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "skyops.name" . }}

To view the agent logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "skyops.name" . }} -f

Your cluster will automatically appear as CONNECTED in the SkyOps Cloud Console.
